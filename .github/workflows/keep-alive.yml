# keep alive test
name: Keep Render Awake

on:
  schedule:
    - cron: "* * * * *"   # cada 1 minuto (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -L --silent --show-error --fail https://poke-catch-uqgj.onrender.com/api/users/ping || true
