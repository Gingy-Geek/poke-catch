# keep alive test
name: Keep Render Awake

on:
  schedule:
    - cron: "*/20 10-22 * * 1-5"   # cada 20 min, 10-22 UTC (7-19 Argentina), lun-vie
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -L --silent --show-error --fail https://poke-catch-uqgj.onrender.com/api/users/ping || true
